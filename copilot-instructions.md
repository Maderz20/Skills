# Copilot Instructions

Primary rules: see `AGENTS.md` at repo root. Read it before doing any work.

When working from a plan, the plan file in `.copilot/plans/` overrides general rules for that session — but only for the specific step being implemented.

On-demand docs in `docs/agent/` are loaded only when the current task matches their topic. Don't read them speculatively.
